export GRAFANA_URL=https://your-slug.grafana.net
export GRAFANA_SA_TOKEN=glsa_your_service_account_token

# Note:
# - GRAFANA_SA_TOKEN is a Grafana Service Account token (Editor/Admin).
# - This is NOT GRAFANA_CLOUD_API_KEY (that one is for Alloy remote_write / Loki / Tempo shipping).
#
# Avoid setting DASHBOARDS_DIR unless you mean to upload dashboards from another repo.
# `deploy/push-dashboards.sh` defaults to `./dashboards` and ignores DASHBOARDS_DIR
# unless you also set `ALLOW_DASHBOARDS_DIR_OVERRIDE=1`.
